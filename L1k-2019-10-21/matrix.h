#pragma once
void show_matrix(int** matrix, int row_count, int col_count);
void create_matrix(int**& matrix, int row_count, int col_count);
void fill_matrix(int** matrix, int row_count, int col_count, int fill_type = 0);
void fill_matrix(int** matrix, int row_count, int col_count);
void clear_matrix(int**& matrix, int row_count);
void clear_matrix(double**& matrix, int row_count);